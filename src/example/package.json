{
  "name": "js-flux-example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "js-flux": "../.."
  },
  "scripts": {
    "update": "cd .. && npm run build:dev && cd example && rm -rf node_modules && yarn upgrade"
  }
}
